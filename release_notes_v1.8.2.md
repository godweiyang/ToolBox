## 改进：WiFi 热力图删除手动模式 + 双指缩放

### 改动
- **删除手动模式**：不再需要点击地图标记，直接自动采样
- **双指捏合缩放**：地图支持 0.3x ~ 8x 缩放，放大后可看清聚集的点
- **单指拖动平移**：放大后可拖动查看不同区域
- **重置视图按钮**：底部新增「重置视图」按钮，一键恢复到自动 fit 状态
- **采样点位置改为世界坐标累加**：每次采样点位置 = 上次位置 + 随机方向 0.3~0.6 米，点自然散开
- **缩放足够大时才显示标签**：避免缩小时文字挤一起
- **网格随缩放自适应**：每 1 米一条网格线，缩太小自动隐藏
