<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 9 及以下写入外部存储 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ 细粒度媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- 分贝仪:麦克风录音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 文件互传:前台服务保活 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 文件互传:WiFi 状态信息（获取局域网 IP） -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 金属探测器:震动报警 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- WiFi 信号探测器:读取 WiFi 信息需要位置权限（Android 6+） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- WiFi 信号探测器:步频检测（Android 10+ 需要 ACTIVITY_RECOGNITION） -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoDownloader"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".VideoDownloaderActivity"
            android:exported="false" />

        <activity
            android:name=".QrCodeActivity"
            android:exported="false" />

        <activity
            android:name=".BiliLoginActivity"
            android:exported="false" />

        <activity
            android:name=".VideoToGifActivity"
            android:exported="false" />

        <activity
            android:name=".NineGridActivity"
            android:exported="false" />

        <activity
            android:name=".GifReverseActivity"
            android:exported="false" />

        <activity
            android:name=".DecibelMeterActivity"
            android:exported="false" />

        <activity
            android:name=".FileShareActivity"
            android:exported="false" />

        <activity
            android:name=".MetalDetectorActivity"
            android:exported="false" />

        <activity
            android:name=".WifiSignalActivity"
            android:exported="false" />

        <!-- 局域网文件互传 HTTP server，前台 Service 保活 -->
        <service
            android:name=".FileShareService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
